name: privacy
on:
  pull_request:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: "1.18.3"
      - uses: actions/checkout@v2
      - run: go build -v ./cmd/dash-client
      - run: ./dash-client
